# 帮会（公会）展示模块 —— 需求文档

> 本文档用于记录「帮会模块」的产品需求与页面逻辑，作为后续编写后端接口、数据库设计、前端页面的统一依据。

---

## 1. 模块定位

帮会模块是站点导航栏中的一个独立模块。用户点击导航栏进入后，可以：

- 浏览所有已存在的帮会（以小卡片列表形式展示）；
- 点击某个帮会小卡片，进入该帮会的详情展示页；
- 若用户尚未注册为某个帮会的"大当家"，则**只能浏览、不能编辑**任何帮会内容；
- 若用户在本模块中注册成为"大当家"，则可以创建/管理**自己的帮会**，填写成员信息、上传成员图片、上传帮会展示图片等内容。

一句话概括：**大当家 = 帮会内容的创建者和管理者；普通用户 = 只读访客。**

---

## 2. 角色与权限

| 角色 | 说明 | 权限 |
|---|---|---|
| 游客 / 普通用户（未注册大当家） | 默认身份 | 只能浏览帮会列表、进入帮会详情页查看内容，不能编辑任何数据 |
| 大当家（已注册） | 通过"注册成为大当家"流程创建，一个大当家对应一个帮会 | 可以创建/编辑自己名下帮会的全部信息：帮会基础信息、背景图、成员信息（含成员图片）、集体合照、帮会照片墙、帮会荣誉、招募信息、管理联系方式 |

**关键权限规则：**
- 一个大当家账号目前对应**一个帮会**（除非后续明确要支持一个人管理多个帮会）。
- 大当家只能编辑自己创建的帮会，不能编辑他人帮会。
- 未注册用户在主页面看到"注册成为大当家"的入口按钮，注册流程需要收集：帮会基本信息 + 大当家自己的身份信息（具体字段后端设计时再细化，前端会根据后端接口来对接）。

---

## 3. 页面结构与信息流（前端展示逻辑，后端需围绕此设计数据结构与接口）

### 3.1 帮会列表页（模块主入口）

- 展示当前所有已注册的帮会，以**小卡片**形式排列（类似列表/网格）。
- 每张小卡片展示帮会的基本缩略信息（如：帮会名称、帮会头图/logo、简短介绍等，具体字段可扩展）。
- 页面同时提供"注册成为大当家"的入口按钮/入口区域。
- 点击某个帮会小卡片 → 跳转进入该帮会的【详情页】。

### 3.2 帮会详情页（点击小卡片后进入）

详情页是一个**可纵向滚动、分区块展示**的页面，从上到下依次为以下几个区块：

#### 区块 A：主视觉区（首屏）
- 展示内容：**帮会成员全身肖像合照** + 大当家设定的**背景图片**。
- 交互：点击合照中的某个成员头像/人像区域 → 弹出（或跳转展示）该成员的**详细信息卡片**，内容包括：
  - 该成员的个人图片（写真/头像）；
  - 该成员的主要信息（如称号、职位、简介等，具体字段留待后端设计时确定）。
- 这是用户进入帮会详情页看到的**第一屏内容**。

#### 区块 B：帮会详细介绍区（继续下滑后展示）
- 帮会的详细文字介绍内容（帮会简介、历史、风格定位等）。
- 帮会**集体合照**。
- 帮会**照片墙 / 帮会相关图片**（多图展示）。

#### 区块 C：帮会荣誉区（可选模块）
- 展示帮会获得过的荣誉/成就列表。
- **该区块为可选内容**：如果大当家没有填写荣誉信息，前端详情页中**不显示"帮会荣誉"这个模块**（即整个区块隐藏，而不是显示"暂无荣誉"之类的占位）。
- 因此后端在数据结构上需要支持"荣誉信息为空"的情况，并且接口返回时需要能让前端判断是否要渲染该区块。

#### 区块 D：招募与联系区（页面最底部）
- 帮会的**招募信息**（是否招人、招募要求、招募说明等）。
- 帮会**管理者（大当家）的详细联系方式**（如联系方式、社交账号等，具体字段留待细化）。

### 页面区块顺序总结（从上到下）：

1. 主视觉区：成员合照 + 背景图（点击成员 → 成员详情弹窗/卡片）
2. 帮会详细介绍 + 集体合照 + 帮会照片墙
3. 帮会荣誉（可选，无内容则整块隐藏）
4. 招募信息 + 管理联系方式

---

## 4. 大当家注册与内容管理（后端优先实现）

大当家在注册/管理后台中需要能够维护以下内容（对应前端详情页各区块）：

1. **帮会基础信息**：帮会名称、简介、背景图、列表页展示用的缩略信息等。
2. **成员信息管理**：
   - 每个成员：个人图片、主要信息（称号/职位/简介等）。
   - 需要支持"成员合照图 + 成员点击热区/坐标或成员列表关联"的方式，让前端知道点击合照的哪个位置对应哪个成员（具体实现方式可在后端设计阶段讨论，比如成员列表 + 排序，前端自行做图片定位；或者后端提供成员在合照中的坐标信息）。
3. **帮会详细介绍内容**：文字介绍、集体合照、照片墙（多图）。
4. **帮会荣誉（可选）**：荣誉列表，可以为空。为空时前端不展示该模块。
5. **招募信息**：是否招人、招募文案等。
6. **管理联系方式**：大当家联系方式信息。

---

## 5. 开发顺序（用户当前诉求）

1. ✅ 先完成**后端**：数据模型、接口（帮会列表、帮会详情、成员详情、大当家注册与内容管理的增删改查接口等）。
2. 前端会依据后端接口文档自行对接开发，暂不需要现在写前端代码。

---

## 6. 待后端设计阶段需要进一步明确的细节（暂不影响本文档记录初始想法，仅作提醒）

- 成员信息与"合照点击热区"的关联方式如何设计；
- 荣誉、招募信息等可选字段的数据结构（空值/null 的表达方式）；
- 大当家与帮会是否严格一对一，还是允许一对多；
- 图片上传的存储方式与限制（数量、大小等）；
- 帮会列表页缩略卡片具体展示哪些字段。

---

*本文档记录的是模块最初的产品构想，作为后续后端接口设计与前端页面还原的依据，请勿在开发过程中偏离以上核心交互逻辑（尤其是"荣誉模块可选隐藏"和"点击成员弹出详情"这两个关键交互点）。*